#!/usr/bin/bash
if [ $# -eq 1 ]; then
	#namaFile = $1
	output=${1%.*}
	ffmpeg -i "$namaFile" -c:v libx264 -c:a copy -preset veryfast -crf 20 ${output}_compressed.mp4 
	echo $output
	echo -e "\nProses kompres selesai!"
fi
